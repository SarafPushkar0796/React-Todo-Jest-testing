// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom

/**
 * We use testing library to make sure the project is:
 * 1. Bug free
 * 2. Increases confidence in the application
 * 3. Speeds up QA time
 * 4. Can serve as documentation
 */

/**
 * Types of testing
 * 1. Unit testing - Is a piece of code that tests a component in complete isolation
 * 2. Integration testing - Is testing the interaction between different components of an application
 */

/**
 * Test block structure
 * 1. Render the component we want to test
 * 2. Find elements we want to interact with
 * 3. Interact with the elements
 * 4. Assert that the results are as expected
 */
import '@testing-library/jest-dom';
